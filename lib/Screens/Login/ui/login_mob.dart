import 'package:flutter/material.dart';

class LoginMobPage extends StatelessWidget {
  const LoginMobPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

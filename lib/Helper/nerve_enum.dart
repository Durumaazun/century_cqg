// ignore_for_file: constant_identifier_names

enum AppPlatform { mobile, web }

enum GreekLicenseValidity { monthly, quarterly }

enum APINames { login }

enum APIErrorCode {
  success,
  noContent,
  faile,
  nodata,
  sqlConectionfaile,
  queryFaile,
  unauthorized,
  sessionRequired,
  requestTimeOut,
  internalServerError,
  invalidUser,
  invalidPassword,
  unknown,
}
